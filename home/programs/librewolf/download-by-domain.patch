diff --git a/chrome/toolkit/content/extensions/parent/ext-downloads.js b/chrome/toolkit/content/extensions/parent/ext-downloads.js
index de38a11..9f1b676 100644
--- a/chrome/toolkit/content/extensions/parent/ext-downloads.js
+++ b/chrome/toolkit/content/extensions/parent/ext-downloads.js
@@ -829,20 +829,19 @@ this.downloads = class extends ExtensionAPIPersistent {
           }
 
           async function createTarget() {
+            let uri = Services.io.newURI(options.url);
             if (!filename) {
-              let uri = Services.io.newURI(options.url);
-              if (uri instanceof Ci.nsIURL) {
-                filename = DownloadPaths.sanitize(
-                  Services.textToSubURI.unEscapeURIForUI(
-                    uri.fileName,
-                    /* dontEscape = */ true
-                  )
-                );
-              }
+              filename = DownloadPaths.sanitize(
+                Services.textToSubURI.unEscapeURIForUI(
+                  uri.fileName,
+                  /* dontEscape = */ true
+                )
+              );
             }
 
-            let target = PathUtils.joinRelative(
+            let target = PathUtils.join(
               await Downloads.getPreferredDownloadsDirectory(),
+              uri.host,
               filename || "download"
             );
 
@@ -862,7 +861,7 @@ this.downloads = class extends ExtensionAPIPersistent {
 
             // Create any needed subdirectories if required by filename.
             const dir = PathUtils.parent(target);
-            await IOUtils.makeDirectory(dir);
+            await IOUtils.makeDirectory(dir, { createAncestors: true });
 
             if (await IOUtils.exists(target)) {
               // This has a race, something else could come along and create
diff --git a/chrome/toolkit/content/global/contentAreaUtils.js b/chrome/toolkit/content/global/contentAreaUtils.js
index 9b8689a..7cfd4b3 100644
--- a/chrome/toolkit/content/global/contentAreaUtils.js
+++ b/chrome/toolkit/content/global/contentAreaUtils.js
@@ -700,6 +700,25 @@ function promiseTargetFile(
     let dir = new FileUtils.File(dirPath);
 
     if (useDownloadDir && dirExists) {
+      let host;
+      try {
+        host = aRelatedURI?.host;
+      } catch {
+        // nsIURI.host can throw if the URI scheme doesn't have a host
+      }
+
+      if (!host) {
+        try {
+          host = aFpP.fileInfo.uri.host;
+        } catch {
+          // nsIURI.host can throw if the URI scheme doesn't have a host
+        }
+      }
+
+      if (host) {
+        dir.append(host);
+      }
+
       dir.append(aFpP.fileInfo.fileName);
       aFpP.file = uniqueFile(dir);
       return true;
diff --git a/modules/HelperAppDlg.sys.mjs b/modules/HelperAppDlg.sys.mjs
index 2a7ba87..2f5dc3b 100644
--- a/modules/HelperAppDlg.sys.mjs
+++ b/modules/HelperAppDlg.sys.mjs
@@ -288,7 +288,7 @@ export class nsUnknownContentTypeDialog {
     }
 
     (async () => {
-      if (!aForcePrompt) {
+      if (aDefaultFileName && !aForcePrompt) {
         // Check to see if the user wishes to auto save to the default download
         // folder without prompting. Note that preference might not be set.
         let autodownload = Services.prefs.getBoolPref(
@@ -301,14 +301,31 @@ export class nsUnknownContentTypeDialog {
           let preferredDir = await Downloads.getPreferredDownloadsDirectory();
           let defaultFolder = new FileUtils.File(preferredDir);
 
+          let host;
           try {
-            if (aDefaultFileName) {
-              result = this.validateLeafName(
-                defaultFolder,
-                aDefaultFileName,
-                aSuggestedFileExtension
-              );
+            host = aLauncher.source.host;
+          } catch {
+            // nsIURI.host can throw if the URI scheme doesn't have a host
+          }
+
+          if (!host) {
+            try {
+              host = BrowsingContext.get(aLauncher.browsingContextId)?.currentURI?.host;
+            } catch {
+              // nsIURI.host can throw if the URI scheme doesn't have a host
             }
+          }
+
+          if (host) {
+            defaultFolder.append(host);
+          }
+
+          try {
+            result = this.validateLeafName(
+              defaultFolder,
+              aDefaultFileName,
+              aSuggestedFileExtension
+            );
           } catch (ex) {
             // When the default download directory is write-protected,
             // prompt the user for a different target file.
@@ -463,13 +480,6 @@ export class nsUnknownContentTypeDialog {
     aAllowExisting = false,
     aAfterFilePicker = false
   ) {
-    if (!(aLocalFolder && isUsableDirectory(aLocalFolder))) {
-      throw new Components.Exception(
-        "Destination directory non-existing or permission error",
-        Cr.NS_ERROR_FILE_ACCESS_DENIED
-      );
-    }
-
     aLeafName = this.getFinalLeafName(aLeafName, aFileExt, aAfterFilePicker);
     aLocalFolder.append(aLeafName);
 
