# Read `man 5 sway` for a complete reference.

### Variables

# Logo key. Use Mod1 for Alt.
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down n
set $up e
set $right i

# Your preferred terminal emulator
set $term alacritty

# Your preferred application launcher
# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
set $menu rofi -modi drun -show drun -matching fuzzy # | xargs swaymsg exec --

### UI

gaps inner 10
default_border none
font pango:cantarell 10

### Output configuration
#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs
output * bg `random-wallpaper` fill

bindsym $mod+equal exec scale-on
bindsym $mod+minus exec scale-off

### Idle configuration
exec swayidle -w \
    timeout 30 'makoctl mode -a sticky' resume 'sh -c \'makoctl mode | grep -q invisible || makoctl mode -r sticky\'' \
    timeout 60 'gnome-pomodoro --pause' resume 'gnome-pomodoro --resume' \
    timeout 300 'swaylock -f --image `random-wallpaper`' \
    timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
    before-sleep 'swaylock -f --image `random-wallpaper`'

### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

### Key bindings
#
# Basics:
#
    # Start a terminal
    bindsym $mod+Return exec $term

    # Kill focused window
    bindsym $mod+q kill

    # Start your launcher
    bindsym $mod+a exec $menu
    bindsym $mod+x exec rofi -modi run -show run -matching fuzzy
    bindsym $mod+w exec rofi -modi window -show window -matching fuzzy
    Bindsym $mod+c exec rofi -modi ssh -show ssh -matching fuzzy
    bindsym $mod+m exec rofimoji

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    bindsym $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+Shift+q exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -b 'Yes, exit sway' 'swaymsg exit'
#
# Moving around:
#
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#
    set $www 1
    set $edit 2
    set $term 3
    set $media 4
    set $games 5
    set $calendar 7
    set $tasks 8
    set $email 9
    set $chat 10

    # Switch to workspace
    bindsym $mod+$www workspace $www
    bindsym $mod+$edit workspace $edit
    bindsym $mod+$term workspace $term
    bindsym $mod+$media workspace $media
    bindsym $mod+$games workspace $games
    bindsym $mod+$calendar workspace $calendar
    bindsym $mod+$tasks workspace $tasks
    bindsym $mod+$email workspace $email
    bindsym $mod+0 workspace $chat

    # Move focused container to workspace
    bindsym $mod+Shift+$www move container to workspace $www
    bindsym $mod+Shift+$edit move container to workspace $edit
    bindsym $mod+Shift+$term move container to workspace $term
    bindsym $mod+Shift+$media move container to workspace $media
    bindsym $mod+Shift+$games move container to workspace $games
    bindsym $mod+Shift+$calendar move container to workspace $calendar
    bindsym $mod+Shift+$tasks move container to workspace $tasks
    bindsym $mod+Shift+$email move container to workspace $email
    bindsym $mod+Shift+0 move container to workspace $chat

    # Move workspace to monitor
    bindsym $mod+Control+Shift+$left move workspace to output left
    bindsym $mod+Control+Shift+$down move workspace to output down
    bindsym $mod+Control+Shift+$up move workspace to output up
    bindsym $mod+Control+Shift+$right move workspace to output right
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+t layout tabbed
    bindsym $mod+d layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+f floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+Control+f focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+comma focus parent

    # Move focus to the child container
    bindsym $mod+period focus child
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+slash move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+slash scratchpad show
#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"
#
# Screenshot:
#
    bindsym Print exec --no-startup-id \
      grim -t png - | wl-copy -t image/png & \
      (play /home/kira/.nix-profile/share/sounds/freedesktop/stereo/screen-capture.oga || \
       play /usr/share/sounds/freedesktop/stereo/screen-capture.oga)

    bindsym Ctrl+Print exec --no-startup-id \
      grim -t png -g "$(slurp)" - | wl-copy -t image/png & \
      (play /home/kira/.nix-profile/share/sounds/freedesktop/stereo/screen-capture.oga || \
       play /usr/share/sounds/freedesktop/stereo/screen-capture.oga)

    bindsym Mod1+Print exec --no-startup-id \
      grim -t png "$HOME/Pictures/Screenshots/$(date +'Screenshot from %Y-%m-%d %T.png')" & \
      (play /home/kira/.nix-profile/share/sounds/freedesktop/stereo/screen-capture.oga || \
       play /usr/share/sounds/freedesktop/stereo/screen-capture.oga)

    bindsym Ctrl+Mod1+Print exec --no-startup-id \
      grim -t png -g "$(slurp)" "$HOME/Pictures/Screenshots/$(date +'Screenshot from %Y-%m-%d %T.png')" & \
      (play /home/kira/.nix-profile/share/sounds/freedesktop/stereo/screen-capture.oga || \
       play /usr/share/sounds/freedesktop/stereo/screen-capture.oga)
#
# Lock:
#
    bindsym $mod+l exec --no-startup-id swaylock --image `random-wallpaper`
#
# Volume:
#
    bindsym XF86AudioRaiseVolume exec --no-startup-id \
      amixer sset Master 5%+ && \
      (play /home/kira/.nix-profile/share/sounds/freedesktop/stereo/audio-volume-change.oga || \
       play /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga)

    bindsym XF86AudioLowerVolume exec --no-startup-id \
      amixer sset Master 5%- && \
      (play /home/kira/.nix-profile/share/sounds/freedesktop/stereo/audio-volume-change.oga || \
       play /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga)

    bindsym XF86AudioMute exec --no-startup-id \
      amixer sset Master toggle

    bindsym Mod1+XF86AudioMute exec --no-startup-id \
      amixer sset Capture toggle
#
# Pomodoro:
#
    bindsym $mod+p exec gnome-pomodoro --start-stop
    bindsym $mod+Shift+p exec gnome-pomodoro --pause-resume

### Start daemons & applications:
#
# https://github.com/swaywm/sway/wiki/GTK-3-settings-on-Wayland
exec_always import-gsettings

exec --no-startup-id blueman-applet
exec --no-startup-id emacs
exec --no-startup-id firefox
exec --no-startup-id gnome-pomodoro --no-default-window --stop
exec --no-startup-id io.gitlab.news_flash.NewsFlash
exec --no-startup-id keepassxc
exec --no-startup-id mako

### Assign windows to workspaces
#
# WWW
assign [app_id="^firefox$"] $www
assign [app_id="^Chromium-browser$"] $www

# Edit
# Emacs GTK3 fork: https://git.savannah.gnu.org/cgit/emacs.git/log/?h=feature/pgtk
assign [app_id="^emacs"] $edit
assign [app_id="^VSCodium"] $edit

# Media
assign [app_id="^mpv$"] $media
assign [app_id="^org.qbittorrent.qBittorrent$"] $media
assign [app_id="^rhythmbox$"] $media
for_window [app_id="^firefox$" title="https://www.youtube.com"] move container to workspace $media

# Games
assign [app_id="^lutris$"] $games
assign [app_id="^org.dolphin-emu.$"] $games
assign [app_id="^org.prismlauncher.PrismLauncher$"] $games
assign [class="^cemu.exe$"] $games
# Non-master Dolphin still uses X11
assign [class="^dolphin-emu$"] $games
assign [class="^Minecraft$"] $games
assign [class="^Steam$"] $games

# Calendar
for_window [app_id="^firefox$" title="https://calendar.google.com"] move container to workspace $calendar
for_window [app_id="^firefox$" title="https://calendar.proton.me"] move container to workspace $calendar

# Tasks
for_window [app_id="^firefox$" title="https://linear.app"] move container to workspace $tasks

# Email & Feeds
assign [app_id="^geary$"] $email
assign [app_id="^io.gitlab.news_flash.NewsFlash$"] $email
for_window [app_id="^firefox$" title="https://mail.google.com"] move container to workspace $email
for_window [app_id="^firefox$" title="https://mail.proton.me"] move container to workspace $email
for_window [app_id="^firefox$" title="https://outlook.office.com"] move container to workspace $email

# Chat
assign [app_id="^Caprine$"] $chat
assign [app_id="^Slack$"] $chat
for_window [app_id="^firefox$" title="https://app.element.io"] move container to workspace $chat
for_window [app_id="^firefox$" title="https://app.slack.com"] move container to workspace $chat

# Picture-in-picture
for_window [app_id="^firefox$" title="^Picture-in-Picture$"] floating enable, sticky enable, border pixel 0, move position 1340 722, opacity 0.95

### Status Bar
#
# Read `man 5 sway-bar` for more information about this section.
bar {
    swaybar_command waybar
}

include /etc/sway/config.d/*
